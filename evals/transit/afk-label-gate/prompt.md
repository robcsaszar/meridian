---
max_turns: 20
allowed_tools: [Read, Glob, Grep, Write, Edit, Skill]
---

Use the `transit` skill for this task.

Run one transit unattended (AFK) on the map `plans/ui-conformance-sweep.md`. Its route items carry only a `needs-triage` label — none carries the route-item label or a mode label.
