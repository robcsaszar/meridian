---
type: llm
weight: 1
---

Judge ONLY this single criterion against the response. Ignore everything else it does well or badly.

**Criterion:** The items are described as end-to-end slices of behaviour, not as horizontal layers of one kind of work (for example not 'write all the schemas', then 'write all the endpoints', then 'write all the tests').

PASS only if the response clearly satisfies it. Absence counts as FAIL.
